--[[
    ELITE SUPREMO V12 - GTA V MOD MENU
    Refatoração completa do código original
    Estilo: Menyoo / Modest (GTA V)
]]

local Players = game:GetService("Players")
local UIS = game:GetService("UserInputService")
local RunService = game:GetService("RunService")
local TweenService = game:GetService("TweenService")
local Lighting = game:GetService("Lighting")

local player = Players.LocalPlayer
local mouse = player:GetMouse()
local camera = workspace.CurrentCamera

-- Configurações (Mantive as suas originais)
local Vars = {
    Aimbot = false,
    SilentAim = false,
    AimbotKey = Enum.UserInputType.MouseButton2,
    AimbotFOV = 520,
    AimbotSmoothness = 0.22,
    Fly = false,
    Noclip = false,
    InfiniteJump = false,
    SpeedHack = false,
    SpeedValue = 165,
    BoxESP = false,
    NameESP = false,
    DistanceESP = false,
    HealthESP = false,
    Tracers = false,
    SkeletonESP = false,
    AutoClicker = false,
    ClickSpeed = 0.05, -- Ajustado para evitar crash
    Fullbright = false,
    SpinBot = false,
    AntiAFK = true,
}

-- ====================== GUI ESTILO GTA V ======================
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "EliteGTA"
ScreenGui.Parent = player:WaitForChild("PlayerGui")
ScreenGui.ResetOnSpawn = false

local Main = Instance.new("Frame")
Main.Size = UDim2.new(0, 260, 0, 480)
Main.Position = UDim2.new(0.05, 0, 0.2, 0)
Main.BackgroundColor3 = Color3.fromRGB(15, 15, 15)
Main.BackgroundTransparency = 0.1
Main.BorderSizePixel = 0
Main.Parent = ScreenGui

-- Banner Superior GTA Style
local Header = Instance.new("Frame")
Header.Size = UDim2.new(1, 0, 0, 75)
Header.BackgroundColor3 = Color3.fromRGB(30, 130, 230) -- Azul GTA
Header.BorderSizePixel = 0
Header.Parent = Main

local Title = Instance.new("TextLabel")
Title.Size = UDim2.new(1, 0, 1, 0)
Title.Text = "ELITE SUPREMO"
Title.TextColor3 = Color3.new(1, 1, 1)
Title.Font = Enum.Font.SourceSansBold
Title.TextSize = 25
Title.Parent = Header

-- Scrolling Container
local Container = Instance.new("ScrollingFrame")
Container.Size = UDim2.new(1, 0, 1, -75)
Container.Position = UDim2.new(0, 0, 0, 75)
Container.BackgroundTransparency = 1
Container.BorderSizePixel = 0
Container.ScrollBarThickness = 2
Container.CanvasSize = UDim2.new(0, 0, 0, 700)
Container.Parent = Main

local ListLayout = Instance.new("UIListLayout", Container)
ListLayout.SortOrder = Enum.SortOrder.LayoutOrder

-- Função de Botão Estilo GTA
local function CreateToggle(name, varName)
    local Btn = Instance.new("TextButton")
    Btn.Size = UDim2.new(1, 0, 0, 38)
    Btn.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
    Btn.BackgroundTransparency = 0.3
    Btn.BorderSizePixel = 0
    Btn.Text = "   " .. name
    Btn.TextColor3 = Color3.new(0.9, 0.9, 0.9)
    Btn.TextXAlignment = Enum.TextXAlignment.Left
    Btn.Font = Enum.Font.SourceSans
    Btn.TextSize = 18
    Btn.AutoButtonColor = false
    Btn.Parent = Container

    local Indicator = Instance.new("Frame")
    Indicator.Size = UDim2.new(0, 4, 0, 25)
    Indicator.Position = UDim2.new(1, -10, 0.5, -12)
    Indicator.BackgroundColor3 = Color3.fromRGB(200, 50, 50) -- Vermelho OFF
    Indicator.BorderSizePixel = 0
    Indicator.Parent = Btn

    Btn.MouseButton1Click:Connect(function()
        Vars[varName] = not Vars[varName]
        Indicator.BackgroundColor3 = Vars[varName] and Color3.fromRGB(50, 200, 50) or Color3.fromRGB(200, 50, 50)
        
        -- Efeito de Seleção GTA
        Btn.BackgroundColor3 = Color3.new(1, 1, 1)
        Btn.TextColor3 = Color3.new(0, 0, 0)
        task.wait(0.05)
        Btn.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
        Btn.TextColor3 = Color3.new(0.9, 0.9, 0.9)
    end)
end

-- ====================== FUNÇÕES CORRIGIDAS ======================

-- SpeedHack Estável
RunService.Heartbeat:Connect(function()
    if player.Character and player.Character:FindFirstChild("Humanoid") then
        player.Character.Humanoid.WalkSpeed = Vars.SpeedHack and Vars.SpeedValue or 16
    end
end)

-- Fly Corrigido (Usando CFrame para atravessar paredes no Noclip)
local bv, bg
RunService.Stepped:Connect(function()
    local char = player.Character
    if Vars.Fly and char and char:FindFirstChild("HumanoidRootPart") then
        if not bv then
            bv = Instance.new("BodyVelocity", char.HumanoidRootPart)
            bv.MaxForce = Vector3.new(math.huge, math.huge, math.huge)
            bg = Instance.new("BodyGyro", char.HumanoidRootPart)
            bg.MaxTorque = Vector3.new(math.huge, math.huge, math.huge)
        end
        
        local dir = Vector3.new()
        if UIS:IsKeyDown(Enum.KeyCode.W) then dir += camera.CFrame.LookVector end
        if UIS:IsKeyDown(Enum.KeyCode.S) then dir -= camera.CFrame.LookVector end
        if UIS:IsKeyDown(Enum.KeyCode.A) then dir -= camera.CFrame.RightVector end
        if UIS:IsKeyDown(Enum.KeyCode.D) then dir += camera.CFrame.RightVector end
        
        bv.Velocity = dir * 100
        bg.CFrame = camera.CFrame
        
        -- Noclip Integrado
        for _, part in pairs(char:GetDescendants()) do
            if part:IsA("BasePart") then part.CanCollide = false end
        end
    else
        if bv then bv:Destroy() bv = nil end
        if bg then bg:Destroy() bg = nil end
    end
end)

-- Aimbot Otimizado
RunService.RenderStepped:Connect(function()
    if Vars.Aimbot and UIS:IsMouseButtonPressed(Vars.AimbotKey) then
        local target = nil
        local dist = Vars.AimbotFOV
        
        for _, p in pairs(Players:GetPlayers()) do
            if p ~= player and p.Character and p.Character:FindFirstChild("Head") then
                local pos, visible = camera:WorldToViewportPoint(p.Character.Head.Position)
                if visible then
                    local magnitude = (Vector2.new(pos.X, pos.Y) - Vector2.new(mouse.X, mouse.Y)).Magnitude
                    if magnitude < dist then
                        dist = magnitude
                        target = p.Character.Head
                    end
                end
            end
        end
        
        if target then
            camera.CFrame = camera.CFrame:Lerp(CFrame.lookAt(camera.CFrame.Position, target.Position), Vars.AimbotSmoothness)
        end
    end
end)

-- Spawn de Opções
CreateToggle("Aimbot (RMB)", "Aimbot")
CreateToggle("Super Speed", "SpeedHack")
CreateToggle("Flight Mode (V)", "Fly")
CreateToggle("Infinite Jump", "InfiniteJump")
CreateToggle("Box ESP", "BoxESP")
CreateToggle("Tracers", "Tracers")
CreateToggle("Fullbright", "Fullbright")
CreateToggle("Anti-AFK", "AntiAFK")

-- Comandos de Teclado
UIS.InputBegan:Connect(function(i, gp)
    if gp then return end
    if i.KeyCode == Enum.KeyCode.K then
        Main.Visible = not Main.Visible
    elseif i.KeyCode == Enum.KeyCode.V then
        Vars.Fly = not Vars.Fly
    end
end)

print("🚀 ELITE SUPREMO V12 - GTA V STYLE CARREGADO!")
